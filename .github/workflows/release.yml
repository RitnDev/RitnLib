name: Release Factorio Mod

on:
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set variables
        run: |
          echo "VERSION=${GITHUB_REF_NAME}" >> $GITHUB_ENV
          echo "MOD_NAME=${GITHUB_REPOSITORY#*/}" >> $GITHUB_ENV

      - name: Verify version matches info.json
        run: |
          INFO_VERSION=$(jq -r '.version' info.json)

          if [ "$INFO_VERSION" != "$VERSION" ]; then
            echo "❌ Version mismatch: tag != info.json"
            exit 1
          fi

      - name: Build Factorio zip
        run: |
          git archive \
            --format=zip \
            --prefix=${MOD_NAME}/ \
            -o ${MOD_NAME}_${VERSION}.zip \
            ${VERSION}

      - name: GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: ${{ env.MOD_NAME }} ${{ env.VERSION }}
          files: "*.zip"

      - name: Upload to Factorio Mod Portal
        run: |
          FILE=${MOD_NAME}_${VERSION}.zip

          RESPONSE=$(curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.FACTORIO_API_KEY }}" \
            -d "mod=${MOD_NAME}" \
            https://mods.factorio.com/api/v2/mods/releases/init_upload)

          UPLOAD_URL=$(echo $RESPONSE | jq -r '.upload_url')

          if [ "$UPLOAD_URL" == "null" ] || [ -z "$UPLOAD_URL" ]; then
            echo "❌ Failed to init upload"
            echo "$RESPONSE"
            exit 1
          fi

          curl -f -X POST \
            -F "file=@${FILE}" \
            "$UPLOAD_URL"